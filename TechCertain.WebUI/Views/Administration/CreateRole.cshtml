@model IEnumerable<TechCertain.WebUI.Models.Permission.GroupViewModel>
<!-- MAIN CONTENT -->
<div id="content">
    <div class="row">
        <!-- NEW WIDGET START -->
        <article class="col-xs-12 col-sm-12 col-md-6 col-lg-6">

            <!-- Widget ID (each widget will need unique ID)-->
            <div class="jarviswidget jarviswidget-color-darken" id="wid-id-0" data-widget-editbutton="false">
                <header>
                    <span class="widget-icon"> <i class="fa fa-table"></i> </span>
                    <h2>Manage Groups </h2>

                </header>

                <!-- widget div-->
                <div>
                    <!-- widget edit box -->
                    <div class="jarviswidget-editbox">
                        <!-- This area used as dropdown edit box -->
                    </div>
                    <!-- end widget edit box -->
                    <!-- widget content -->
                    <div class="widget-body no-padding">
                        <table id="tableGroups" class="table table-striped table-bordered table-hover" width="100%">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @*@foreach (var item in Model) {
                                        <tr>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.Name)
                                            </td>
                                            <td>
                                                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                                                @Html.ActionLink("Permissions", "GroupRoles", new { id=item.Id }) |
                                                <a href="javascript:void(0);" onclick="DeleteGroup('@item.Id');">Delete</a>
                                            </td>
                                        </tr>
                                    }*@
                            </tbody>
                            <tfoot>
                                <tr>
                                    @*<td colspan="2">@Html.ActionLink("Create New Group", "Create")</td>*@
                                    <td colspan="2">
                                        <a onclick="$('#newGroupModal').show();" aria-hidden="true" style="color:green" data-toggle="modal"><ul>Create New Group</ul></a>

                                        @*<a href="#confirmDelete" aria-hidden="true" style="color:red" data-toggle="modal" data-boat-delname="@item.BoatName"><span class="glyphicon glyphicon-trash"></span></a>*@

                                        @*<a href="javascript:void(0);" onclick="ShowCreateModal();">Create New Group</a>*@
                                    </td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>
        </article>

    </div>
</div>
<!-- END MAIN CONTENT -->
<!-- Edit Boat -->

<div class="modal" id="newGroupModal" tabindex="-1" role="dialog" data-backdrop="static">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" onclick="$('#newGroupModal').hide()">&times;</button>
                <h4 class="modal-title">Group Details</h4>
            </div>
            <div class="modal-body">
                <div class="row">

                    <section class="col col-xs-12 col-sm-12 col-md-12 col-lg-12">
                        <section class="col col-xs-3 col-sm-3 col-md-3 col-lg-3">

                            <label>Group Name</label>
                        </section>
                        <section class="col col-xs-9 col-sm-9 col-md-9 col-lg-9">
                            <label class="input ">
                                <input data-type="TEXTBOX" data-name="groupName" id="groupName" name="Name" placeholder="Group Name" value="">
                            </label>
                        </section>
                    </section>

                </div>
            </div>
            <div class="modal-footer">
                <section class="col col-xs-12">
                    <button onclick="AddMarinaLocModal('#marinaName'); $('#addMarinaModal').hide();" class="btn-sm ">Add</button>
                </section>
            </div>
        </div>
    </div>
</div>



<div id="newGrousdspModal" class="modal fade " role="dialog">
    <div class="modal-content">

        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal">&times;</button>
            <h4 class="modal-title" style="color:blue; font-size:22px">Add New Group</h4>
        </div>
        <div class="modal-body">
            @*<form id="GroupForm">

                <div class="form-group">
                    <label class="col-md-2 control-label">@Html.DisplayNameFor(model => model.Name)</label>
                    <div class="col-md-10">
                        <input class="form-control" name="Name" value="" placeholder="Enter Group name here" type="text">
                    </div>
                </div>*@

            @*<div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Add" class="btn btn-default" />
                    </div>
                </div>*@

            @*</form>*@

            <h1> hellow</h1>
        </div>
        <div class="modal-footer">
            <input type="submit" value="Add" class="btn btn-default" />

            <a href="#" class="btn btn-danger" data-dismiss="modal">Cancel<i class="fa fa-close"></i></a>
        </div>
    </div>
</div>



@section pagespecific {
    @*<script src="/Scripts/datatables"></script>*@

    <script type="text/javascript">
$(document).ready(function () {
	//$('#tableGroups').DataTable({
	//	"sDom": "<'dt-toolbar'<'col-xs-12 col-sm-6'f><'col-sm-6 col-xs-12 hidden-xs'l>r>" +
 //               "t" +
 //               "<'dt-toolbar-footer'<'col-sm-6 col-xs-12 hidden-xs'i><'col-xs-12 col-sm-6'p>>",
 //           	"autoWidth": true
	//})
});

function ShowCreateModal()
{
    debugger;
	$('#newGroupModal').show();
}

function DeleteGroup(groupId)
{
	if (confirm("Delete this Group?") == false)
		return;

	$.ajax({
     	// change back once mono decides to fix
        url: '@Url.Action("Delete", "Groups")',
        type: "POST",
	    data: { Id: groupId}
    })
    .done(function (data) {
        $.smallBox({
            title: "Success",
            content: "The group has been removed",
            color: "#5F895F",
            iconSmall: "fa fa-check bounce animated",
            timeout: 4000
        });
		location.reload();
    })
    .fail(function (xhr, httpStatusMessage, customErrorMessage) {
        alert(customErrorMessage);
    });
}
    </script>
}