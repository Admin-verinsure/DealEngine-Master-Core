@model TechCertain.WebUI.Models.Product.ProductRisksVM

<div id="content">
    <!-- Bread crumb is created dynamically -->
    <!-- row -->
    <div class="row">
        <!-- col -->
        <div class="col-xs-12 col-sm-7 col-md-7 col-lg-4">
            <h1 class="page-title txt-color-blueDark">
                <!-- PAGE HEADER -->
                <i class="fa-fw fa fa-file-o"></i> Products <span>
                    >
                    Find Products
                </span>
            </h1>
        </div>
        <!-- end col -->
    </div>
    <!-- widget grid -->
    <section id="widget-grid" class="">
        <!-- row -->
        <div class="row">
            <!-- NEW WIDGET START -->
            <article class="col-sm-12 col-md-12 col-lg-6">
                <!-- Widget ID (each widget will need unique ID)-->
                <div class="jarviswidget" id="wid-id-1" 
                     data-widget-colorbutton="false" 
                     data-widget-editbutton="false" 
                     data-widget-togglebutton="false" 
                     data-widget-deletebutton="false" 
                     data-widget-custombutton="false" 
                     data-widget-sortable="false">
                    <header>
                        <h2>Find Products</h2>
                    </header>

                    <div>
                        <div class="widget-body no-padding smart-form">
	                		<form method="POST" id="product search-form" class="smart-form client-form">
								@*@(Html.InputTableFor<TechCertain.WebUI.Models.RiskEntityViewModel>()
								    .Columns(column =>
								    {
								        column.Expression(p => p.Insured).Title("");
								        column.Expression(p => p.Id).InputType("hidden").Visible(false);
								        column.Expression(p => p.CoverAll).Title("All").InputType("checkbox");
								        column.Expression(p => p.CoverLoss).Title("Loss").InputType("checkbox");
								        column.Expression(p => p.CoverInterruption).Title("Interuption").InputType("checkbox");
								        column.Expression(p => p.CoverThirdParty).Title("Liability to others").InputType("checkbox");
								    })
								    .DataSource(this.Model))
		                   		<footer>
									<button type="submit" class="btn btn-primary">
									    Search
									</button>
		                    	</footer>*@
							</form>
						</div>
					</div>
				</div>
			</article>
		</div>
	</section>
</div>